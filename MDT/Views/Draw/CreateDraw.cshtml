@using MDT.Models.DTO
@using MDT.ViewModels
@model Tuple<DrawVM, List<DrawTypeDTO>, string>
@{
    bool isPast = (bool)Session["past"];
    ViewBag.Title = "Add a Drawing";
}
<div id="addForm">
    @Html.Partial("CreateEditDrawForm", Model)
</div>
